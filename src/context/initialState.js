const user = {
  userTokens: {
    AuthenticationResult: {
      IdToken: '',
    },
    newUserTokens: {},
  },
}
const User = {}
const Role = {}

const organisation = {
  Organisations: [],
}

export { user, organisation, User, Role }
